<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Afnan Photo Gallery</title>
  <style>
    /* ---------- GLOBAL THEME (WHITE) ---------- */
    :root {
      --primary: #111;
      --secondary: #555;
      --accent: #eaeaea;
      --shadow: 0 10px 30px rgba(0,0,0,0.08);
      --radius: 16px;
    }

    * {
      box-sizing: border-box;
      font-family: "Segoe UI", system-ui, -apple-system, BlinkMacSystemFont;
    }

    body {
      margin: 0;
      background: white;
      color: var(--primary);
    }

    /* ---------- HEADER ---------- */
    header {
      position: sticky;
      top: 0;
      z-index: 100;
      background: white;
      border-bottom: 1px solid #eee;
      padding: 16px 32px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    header h1 {
      font-size: 22px;
      margin: 0;
    }

    nav button {
      background: black;
      color: white;
      border: none;
      padding: 10px 18px;
      border-radius: 999px;
      cursor: pointer;
      transition: 0.3s;
    }

    nav button:hover {
      opacity: 0.85;
    }

    /* ---------- HERO ---------- */
    .hero {
      padding: 80px 20px;
      text-align: center;
      animation: fadeIn 1.2s ease;
    }

    .hero h2 {
      font-size: 42px;
      margin-bottom: 12px;
    }

    .hero p {
      color: var(--secondary);
      max-width: 700px;
      margin: auto;
    }

    /* ---------- SUBJECT FILTER ---------- */
    .subjects {
      display: flex;
      gap: 10px;
      justify-content: center;
      flex-wrap: wrap;
      margin: 30px 0;
    }

    .subjects button {
      border: 1px solid #ddd;
      background: white;
      padding: 8px 16px;
      border-radius: 999px;
      cursor: pointer;
      transition: 0.3s;
    }

    .subjects button:hover {
      background: var(--accent);
    }

    /* ---------- GALLERY ---------- */
    .gallery {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 24px;
      padding: 40px;
    }

    .card {
      background: white;
      border-radius: var(--radius);
      overflow: hidden;
      box-shadow: var(--shadow);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .card:hover {
      transform: translateY(-8px) scale(1.02);
      box-shadow: 0 20px 40px rgba(0,0,0,0.12);
    }

    .card img {
      width: 100%;
      height: 260px;
      object-fit: cover;
      transition: transform 0.4s;
    }

    .card:hover img {
      transform: scale(1.08);
    }

    .card .info {
      padding: 16px;
    }

    .card .info h3 {
      margin: 0 0 6px;
    }

    .card .info span {
      font-size: 13px;
      color: var(--secondary);
    }

    /* ---------- ADMIN PANEL ---------- */
    .admin {
      display: none;
      padding: 40px;
      border-top: 1px solid #eee;
      animation: slideUp 0.6s ease;
    }

    .admin h2 {
      margin-top: 0;
    }

    .admin input, .admin select {
      padding: 12px;
      width: 100%;
      margin-bottom: 12px;
      border-radius: 12px;
      border: 1px solid #ddd;
    }

    .admin button {
      padding: 12px 18px;
      border-radius: 12px;
      border: none;
      background: black;
      color: white;
      cursor: pointer;
    }

    /* ---------- FOOTER ---------- */
    footer {
      text-align: center;
      padding: 30px;
      border-top: 1px solid #eee;
      color: var(--secondary);
    }

    /* ---------- ANIMATIONS ---------- */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes slideUp {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>

<header>
  <h1>Afnan Photo</h1>
  <nav>
    <button onclick="login()">Login / Sign in</button>
  </nav>
</header>

<section class="hero">
  <h2>Creative Photo Showcase</h2>
  <p>A clean, modern white-themed gallery to display and manage beautiful photos. Only admin can upload and manage images.</p>
</section>

<div class="subjects">
  <button>Afnan Photo</button>
  <button>Photo</button>
  <button>Nature</button>
  <button>Portrait</button>
</div>

<section class="gallery" id="gallery">
  <!-- Example Card -->
  <div class="card">
    <img src="https://images.unsplash.com/photo-1500530855697-b586d89ba3ee" />
    <div class="info">
      <h3>Sample Photo</h3>
      <span>Afnan Photo</span>
    </div>
  </div>
</section>

<!-- ADMIN PANEL (ONLY FOR ADMIN GMAIL) -->
<section class="admin" id="adminPanel">
  <h2>Admin Upload Panel</h2>

  <input type="file" id="fileInput" accept="image/*" />
  <input type="text" id="imageLink" placeholder="Or paste image URL" />

  <select id="subject">
    <option>Afnan Photo</option>
    <option>Photo</option>
    <option>Nature</option>
    <option>Portrait</option>
  </select>

  <button onclick="addPhoto()">Upload Photo</button>
</section>

<footer>
  <p>— Website made by Alif and ChatGPT —</p>
</footer>

<script>
  const ADMIN_EMAIL = "mraeroxol@gmail.com";

  function login() {
    const email = prompt("Enter your Gmail:");
    if (!email) return;

    if (email === ADMIN_EMAIL) {
      alert("Welcome Admin!");
      document.getElementById("adminPanel").style.display = "block";
    } else {
      alert("Login successful. Viewer mode only.");
    }
  }

  function addPhoto() {
    const link = document.getElementById("imageLink").value;
    const subject = document.getElementById("subject").value;

    if (!link) {
      alert("Please provide an image link (demo mode).");
      return;
    }

    const card = document.createElement("div");
    card.className = "card";
    card.innerHTML = `
      <img src="${link}" />
      <div class="info">
        <h3>New Photo</h3>
        <span>${subject}</span>
      </div>
    `;

    document.getElementById("gallery").prepend(card);
    document.getElementById("imageLink").value = "";
  }
</script>

</body>
</html>
